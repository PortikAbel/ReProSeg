# ReProSeg with DeepLab v3 backbone configuration

backbone_network: deeplab_v3
checkpoint: "/home/pabel/nni-experiments/fz4v5qu8/environments/local-env/trials/mEiRF/checkpoints/net_trained_best_miou"
# backbone_checkpoint: "/data/DeepLab_checkpoints/best-epoch=31-val_mIoU=0.713.ckpt"

bias: false

disable_pretrained: false
train_backbone_during_pretrain: false

# Loss configuration
criterion: dice

loss_weights:
  alignment: 0.0
  jsd: 0.0
  tanh: 0.0
  classification: 1.0